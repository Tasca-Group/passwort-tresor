[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python -c 'from server import init_db; init_db()' && gunicorn server:app --bind 0.0.0.0:$PORT"
